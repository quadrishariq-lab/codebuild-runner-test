name: codebuild-runner-test2

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on:
codebuild-simple-runner-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
name: Print job1 details
        run: |
          echo "THIS_IS=job1"
          echo "RUN_ID=${{ github.run_id }}"
          echo "JOB=${{ github.job }}"
          sleep 30

  job2:
    runs-on:
codebuild-simple-runner-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
name: Print job2 details
        run: |
          echo "THIS_IS=job2"
          echo "RUN_ID=${{ github.run_id }}"
          echo "JOB=${{ github.job }}"
          sleep 30
